import { Route, Switch } from 'react-router-dom';
function App() {
  return (
    <div className="App">
      <Switch>
        <Route path="/" exact component={} />
        <Route path="/play" exact component={} />
      </Switch>
    </div>
  );
}

export default App;
