import React from 'react'
import PropTypes from 'prop-types'
import GamePlay from './GamePlay'

const Fix = ({ grid }) => {
  console.log(grid)
  return <div>fvnnvn</div>
}

GamePlay.propTypes = {
  grid: PropTypes.string,
}
export default Fix
